import React from 'react'
import { Global, css } from '@emotion/react'

const GlobalStyles = () => {
    return <Global
        styles={css`
            
        `}
    />
}

export default GlobalStyles